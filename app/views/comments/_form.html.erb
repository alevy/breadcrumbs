<%= form_for([comment.post ,comment]) do |f| %>
  <% if comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field required">
    <%= f.label :name %><br/>
    <%= f.text_field :name, :placeholder => "Jane Doe" %>
  </div>
  
  <div class="field required">
    <%= f.label :email, "E-mail" %><br/>
    <%= f.text_field :email, :placeholder => "jdoe@example.com" %>
  </div>
  
  <div class="field">
    <%= f.label :website %><br/>
    <%= f.text_field :website, :placeholder => "http://www.example.com" %>
  </div>

  <div class="field required">
    <%= f.label :comment %><br/>
    <%= f.text_area :comment %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
